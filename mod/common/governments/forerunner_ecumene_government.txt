# Intentional dependency override: preserves its public government ID for saved empires and presets.
gov_ecumene_council = {
	ruler_title = RT_FIRST_COUNCILOR
	ruler_title_female = RT_FIRST_COUNCILOR
	possible = {
		# Keep the dependency's pre-scripted country valid while making the custom authority selectable.
		OR = {
			has_authority = auth_forerunner_ecumene
			has_authority = auth_oligarchic
		}
		has_civic = civic_forerunner_ecumene
	}
	weight = { base = 5000 }
}
